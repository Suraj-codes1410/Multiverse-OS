# Mobile Directory

This directory houses mobile-optimized interfaces, touch gestures, and light-weight responsive adaptations of desktop views.

## Usage Guidelines
* Use this folder to isolate mobile-only screens, responsive drawer replacements, and mobile touch swipe controls.
* If a component works responsively across desktop and mobile, keep it in `desktop/` or `shared/` instead of duplicating it here.

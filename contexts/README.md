# Contexts Directory

This directory houses React Context declarations and type interfaces.

## Usage Guidelines
* Code here must represent the definition and typings of React Contexts (e.g. `ShellContext`, `AudioContext`).
* Keep active state logic and useEffect loops inside the `providers/` folder, importing only context blueprints from here.

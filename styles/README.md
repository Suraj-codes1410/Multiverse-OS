# Styles Directory

This directory stores global and module-level CSS styles.

## Usage Guidelines
* Put global overrides, page fade transitions, scrollbar visual styles, and utility classes here.
* Main entry point config files are mapped here to feed the Tailwind compilation layers.

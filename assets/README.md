# Assets Directory

This directory stores static, build-time frontend assets (e.g. SVG graphics, inline vector assets, UI background overlays).

## Usage Guidelines
* Code-based assets like SVGs or local PNG placeholders go here.
* For public assets served dynamically on the web (such as candidate resumes and favicon.ico files), use the root `/public` directory instead.

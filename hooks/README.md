# Hooks Directory

This directory houses custom React state hooks and event hooks.

## Usage Guidelines
* Code here should represent isolated React lifecycles and event listeners (e.g. keyboard listeners, window resize hooks, API fetch state wrappers).
* Custom hooks must start with the `use` prefix (e.g. `useOracle`, `useKeyPress`).

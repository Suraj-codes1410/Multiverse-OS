# Providers Directory

This directory houses React layout context providers and application state wrappers.

## Usage Guidelines
* Use this folder for root-level layout wrappers that configure global state or telemetry providers (e.g. `ShellProvider`, `ThemeProvider`, `AnalyticsProvider`).
* Providers should configure React context contexts.

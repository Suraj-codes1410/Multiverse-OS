# Layout Directory

This directory houses high-level structural containers that define the page flow, layouts, and grid structures.

## Usage Guidelines
* Use this folder for components that establish page layouts or section divisions (e.g. `Navbar`, `Footer`, `Container`, `Section`).
* Layout components should remain presentationally focused and be reuse-friendly across different route page setups.

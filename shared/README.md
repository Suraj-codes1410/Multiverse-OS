# Shared UI Directory

This directory houses atomic, presentation-only, highly reusable visual UI components and icons.

## Usage Guidelines
* Components placed here must be generic, accepting custom classnames and callback props.
* Do not put business logic or routes inside this folder. Examples include: `Button`, `Card`, `Badge`, `Icons`, `MarkdownRenderer`.
